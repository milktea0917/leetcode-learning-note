class Solution:
    def nextGreatestLetter(self, letters: List[str], target: str) -> str:
        
        letters_sort = sorted(list(set(letters)))

        for index, value in enumerate(letters_sort):
            if value > target:
                return value

        return letters[0]
