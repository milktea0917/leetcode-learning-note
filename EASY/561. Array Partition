class Solution:
    def arrayPairSum(self, nums: List[int]) -> int:

        nums = sorted(nums)

        final_return = 0
        for i in range(len(nums)//2):
            final_return += nums[2*i]

        return final_return
        
